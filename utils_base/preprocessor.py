import numpy as np
from imageio import imread
from PIL import Image
import numpy as np


def preprocess_input(x, v2=True):
    x = x.astype('float32')
    x = x / 255.0
    if v2:
        x = x - 0.5
        x = x * 2.0
    return x
#标准化
